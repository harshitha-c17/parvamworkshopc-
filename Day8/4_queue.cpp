#include<iostream>
#include<queue>// Required header

int main(){
    //create a queue of integers
    std::queue<int> q; //fifo

    //1.Enqueue elements
    q.push(10);
    q.push(20);
    q.push(30);

    //2.Check front and back elements
    
}